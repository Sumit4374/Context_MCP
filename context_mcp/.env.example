# Context MCP — Environment Variables
# Copy this file to .env and fill in values.
# NEVER commit .env with real credentials.

# PostgreSQL
POSTGRES_PASSWORD=context_dev_password

# API Authentication (required when api-token-enabled=true)
CONTEXT_MCP_API_TOKEN=change-me-in-production
